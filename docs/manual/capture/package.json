{
  "name": "elicit-admin-manual-capture",
  "private": true,
  "version": "1.0.0",
  "description": "Regenerates the administrator's manual screenshots (UC-029) from a running Admin on the umbrella compose stack, creating the console state a fresh install lacks. Kept out of the Vaadin-managed package.json at the project root.",
  "type": "module",
  "scripts": {
    "capture": "playwright install --with-deps chromium && node capture-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
